
# Exported from Render on 2024-08-31T12:16:16Z
databases:
  - name: postgres
    databaseName: postgres_p6kt
    user: postgres_p6kt_user
    plan: free
    region: oregon
    ipAllowList: []
    postgresMajorVersion: "16"
version: "1"

services:
  - type: web
    name: agenda-virtual
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "bundle exec rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "bundle exec rails server"
    envVars:
      - key: RAILS_MASTER_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString

